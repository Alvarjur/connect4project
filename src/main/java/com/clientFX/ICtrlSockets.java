package com.clientFX;

public interface ICtrlSockets {
    // para colocar la ficha en el placeholder cercano
    void setDiscOnCloserPlaceholder();

    // para atualizar la estructura de datos que guarda la información de las fichas en la graella (una matriz por ejemplo)
    void update
}
